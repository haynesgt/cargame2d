{
	"scripts": {
		"clean": "rm -r build",
		"build": "cp -r src/ build/ && cp src/static/* build/ && webpack",
		"dev": "http-server -p ${PORT-9999}",
		"watch-ts": "webpack --watch"
	},
	"dependencies": {
		"@types/lodash": "^4.14.191",
		"ts-loader": "^9.4.2",
		"typescript": "^4.9.4",
		"webpack": "^5.75.0",
		"webpack-cli": "^5.0.1"
	}
}
